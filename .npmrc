# Repository-wide policy: no lock files during early development
# pnpm settings
save-lockfile=false
prefer-frozen-lockfile=false

# npm settings (in case npm is used)
package-lock=false
# pnpm configuration for monorepo root
